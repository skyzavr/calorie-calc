import { Header } from '@widgets/header';
import { Layout } from '@shared/ui/Layout/Layout';

export const MainLayout = () => {
  return (
    <Layout>
      <Header />
    </Layout>
  );
};
